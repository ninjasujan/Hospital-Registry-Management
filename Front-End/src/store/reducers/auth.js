const initialState = {
  token: null,
  loading: false,
  error: null,
};

const authReducers = (state = initialState, action) => {
  switch (action.type) {
    case 1:
      break;
    default:
      return state;
  }
};

export default authReducers;
